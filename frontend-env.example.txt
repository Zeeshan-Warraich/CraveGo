# Add these entries to .env.local in the CraveGo project root (next to package.json).
# Merge into an existing file if you have one. Do not place these in backend/.env.
VITE_GOOGLE_MAPS_API_KEY=YOUR_RESTRICTED_BROWSER_API_KEY
# DEMO_MAP_ID is for local testing. Create your own JavaScript map ID for production.
VITE_GOOGLE_MAPS_MAP_ID=DEMO_MAP_ID
